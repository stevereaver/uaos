# UAOS timezone configuration
# Specify an IANA timezone name (e.g. America/New_York, Europe/London, Asia/Tokyo)
# The timezone is applied by ntpd at startup to display local time.
# The CMOS RTC always stores UTC internally.
Australia/Sydney
