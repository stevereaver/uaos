/* cmd_rename.c — C:rename — rename or move a file */

#include "cmd_internal.h"

void Cmd_Rename(NativeCmdCtx *ctx, const char *args)
{
    (void)args;
    PRINT("Rename not yet implemented - use copy and delete");
}
